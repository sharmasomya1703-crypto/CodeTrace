/** Client-side sorting algorithm ids. Full metadata comes from the API. */
export const DEFAULT_SORT_ALGOS = [
  "bubble-sort",
  "insertion-sort",
  "merge-sort",
  "quick-sort",
] as const;
